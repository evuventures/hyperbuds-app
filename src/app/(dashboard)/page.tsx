import MainContent from "./dashboard/page";

/**
 * Root "/" – same content as /dashboard, rendered inside the shared dashboard layout.
 */
export default function HomePage() {
  return <MainContent />;
}
